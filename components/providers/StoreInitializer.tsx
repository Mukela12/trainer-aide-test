"use client";

/**
 * StoreInitializer — placeholder component
 *
 * All server-state stores have been migrated to React Query hooks,
 * which auto-fetch on mount. This component is retained as a no-op
 * in case future Zustand UI-only stores need global initialization.
 */
export function StoreInitializer() {
  return null;
}
